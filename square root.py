"""
Write a three-line program that prompts for a number, converts the input to an integer,
and calculates and prints its square root.
"""
import math
print("Enter the number of which you want to find the square root")
a=input()
b=int(a)
print(math.sqrt(b))
